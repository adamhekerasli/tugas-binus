#include <stdio.h>

int main () {
    int length = 5;
    int width = 3;
    int area = 0;

    area = length * width;

    printf("Length = %d\n", length);
    printf("Width = %d\n", width);
    printf("Area of rectangle = %d\n", area);


    return 0;
}